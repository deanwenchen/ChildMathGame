# 项目：儿童算数小能手 - 核心逻辑协议 (V3.0)

## 0. 启动与唤醒协议 (Boot & Context Recovery)
- **强制初始化**：对话开始或重启时，必须读取 `claude.md`, `docs/pedagogy.md`, `task_plan.md`, `progress.md`。
- **状态报告**：输出 [状态对齐] 报告（目标、进度、教学对齐）。
- **抗遗忘巡检**：每进行 3 次任务迭代，必须主动重新读取一次 `docs/pedagogy.md`，确保逻辑未偏离教育初衷。

## 1. 技能集成与分工
- **Planning-with-Files (流程引擎)**: 维护 [Plan -> Findings -> Code -> Progress] 闭环。
- **NotebookLM-Skill (知识对齐)**: 确保所有教育逻辑（难度、反馈、梯度）皆有据可查，严禁背离 `pedagogy.md`。
- **Superpowers (头脑风暴与深度推理)**: 
  - **头脑风暴**：在接受新需求时，先提供 3 个不同维度的创意方案。
  - **交互审查**：利用多模态推演儿童使用时的视觉与心理反馈。

## 2. 标准行为准则 (强化执行循环)

### 第一阶段：需求对齐与头脑风暴 (Brainstorming)
- **动作**：收到需求后，禁止立即更新计划。
- **产出**：利用 **Superpowers** 提出三个方案（如：极简版、趣味加强版、竞技挑战版），并结合 **NotebookLM-Skill** 说明哪个方案最符合 `pedagogy.md`。
- **用户确认**：待用户选定方案后，进入下一阶段。

### 第二阶段：结构化执行 (Standard Workflow)
1. **更新计划 (Planning)**: 
   基于选定方案更新 `task_plan.md`。
2. **记录发现 (Findings)**: 
   在修改代码前，必须写入 `findings.md`。包含：
   - 方案选择理由。
   - **交互风险预测**：(例：儿童可能会对“退位减法”感到挫败，需增加动效引导)。
   - **逻辑边界**：明确数字取值范围等约束。
3. **修改代码 (Execution)**: 
   编写代码，保持代码注释符合教学逻辑。
4. **同步进度 (Sync)**: 
   更新 `progress.md`。

## 3. 运行限制 (Guardrails)
- **禁止“指令复述”**：不要只是复述我的指令，必须提供基于 **Superpowers** 的增量建议。
- **禁止“记忆断层”**：若发现上下文过长导致逻辑模糊，必须请求用户确认核心文件内容。
- **教学优先级**：若功能创意与 `pedagogy.md` 冲突，以 `pedagogy.md` 为准。
- **强制停顿 (Breakpoints)**：在完成“方案发散”后，**必须停止自动化流程**，等待用户输入 `Confirm` 或 `Select Plan 1/2/3`，严禁在未获授权时擅自修改 `src/` 目录。
- **文件原子性**：每次 `git commit` 前，确保 `progress.md` 已记录了本次变更的教育意义。